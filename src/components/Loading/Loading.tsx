import React from 'react';
import { ActivityIndicator } from 'react-native';

const Loading: React.FC = () => {
  return <ActivityIndicator color="#A155C7" size="large" />;
};

export default Loading;
